{
  "name": "estaterentals",
  "version": "1.0.0",
  "private": true,
  "description": "JerryHomes / EstateRentals — combined Next.js + Express on one Render Web Service",
  "main": "server.js",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend && npm install",
    "build:backend": "npm run db:generate --prefix backend && npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "build": "npm run build:backend && npm run build:frontend",
    "start": "NODE_ENV=production node server.js",
    "start:combined": "NODE_ENV=production node server.js",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "db:generate": "npm run db:generate --prefix backend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "db:push": "npm run db:push --prefix backend",
    "db:seed": "npm run db:seed --prefix backend"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "next": "^15.3.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
